{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=(JSON.parse(require('fs').readFileSync(0,'utf8')).tool_input||{}).file_path||'';if(/\\.env(\\.[A-Za-z0-9_.-]+)?$/.test(p)){console.error('Edition des fichiers .env bloquee (cles Supabase). Modifie-les a la main si necessaire.');process.exit(2)}\""
          }
        ]
      }
    ]
  }
}
