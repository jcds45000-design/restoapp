node_modules
dist
.DS_Store
*.local
.env
.env.local
.env.production

# Scripts de test/vérification locaux : contiennent des identifiants en clair.
# NE JAMAIS committer (le dépôt est public). À migrer vers des variables d'env.
pw_*.cjs
verif_*.mjs
extract*.mjs
cleanup_taches.mjs

# Sessions de brainstorming visuel (maquettes locales)
.superpowers/

# Migration stock : contient fournisseurs et prix d'achat (dépôt public)
supabase_migration3_stock.sql

# Captures et artefacts de vérification locale
.verif*/

# Doc d'exploitation : infos internes de passation, ne pas publier (dépôt public)
docs/EXPLOITATION.md
